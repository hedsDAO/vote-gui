"use client";

import { GridItem as GridItemBox, GridItemProps } from "@chakra-ui/react";

const GridItem = (props: GridItemProps) => {
  return <GridItemBox {...props} />;
};

export default GridItem;
